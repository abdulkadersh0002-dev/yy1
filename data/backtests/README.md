# Backtest Data Directory

Place historical price bars and signal files used by the backtesting harness in this directory. Update
`config/backtest.config.json` with the correct file names, formats, and metadata before running
`npm run backtest:run`.
