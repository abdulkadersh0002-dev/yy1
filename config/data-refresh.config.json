{
  "$schema": "./schemas/data-refresh.schema.json",
  "description": "Runtime data refresh workflow configuration",
  "etlConfigPath": "./historical-warehouse.config.json",
  "apiBaseUrl": "http://localhost:4101",
  "freshnessThresholdMinutes": 10,
  "notes": [
    "Populate config/historical-warehouse.config.json with price, macro, and news sources before running the refresh script.",
    "Set apiBaseUrl to the running backend address (include protocol and port).",
    "The script exits with a non-zero code if endpoints are stale or unreachable."
  ]
}
