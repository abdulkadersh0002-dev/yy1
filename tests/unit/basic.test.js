import test from 'node:test';
import assert from 'node:assert/strict';

// Placeholder sanity check to ensure unit test harness stays wired up.
test('unit harness sanity check', () => {
  assert.strictEqual(1 + 1, 2);
});
